name: 19 - managing concurrency

on:
    workflow_dispatch

jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Ping URL
              id: ping-url
              uses: ./.github